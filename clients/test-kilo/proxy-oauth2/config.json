{
  "http_addr": ":8080",
  "jwks_url": "http://localhost:9000/.well-known/jwks.json",
  "required_claims": ["sub", "aud"],
  "allowed_audiences": ["llm-service"],
  "allowed_hosts": ["localhost", "127.0.0.1", "llm.local"],
  "allow_unsigned": true,
  "allowed_methods": ["GET", "POST", "PUT", "DELETE", "PATCH"],
  "allowed_path_prefix": "/"
}
